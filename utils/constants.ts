export const DEFAULT_CHAT_MESSAGE = "It seems like you'd like me to write some React code, but I need a little more information on what kind of application or component you're interested in. Here's an example of a simple \"Guessing Game\" in React: ```jsx import React, { useState } from 'react'; function GuessingGame() { const [guess, setGuess] = useState(0); const [result, setResult] = useState(null); const [numberToGuess, setNumberToGuess] = useState(Math.floor(Math.random() * 101)); // generate a random number between 1 and 100 const handleGuess = (value) => { if (!isNaN(value)) { let correctGuesses = 0; if (value < numberToGuess) { result = 'Too low, try again! '; setNumberToGuess(Math.max(1, numberToGuess - Math.floor(value / 10))); } else if (value > numberToGuess) { result = 'Too high, try again!'; setNumberToGuess(1 + Math.min(100, numberToGuess + Math.ceil((value - numberToGuess) / 10))); } correctGuesses = value === numberToGuess; setResult(result); } else if (value !== '') { result = 'Invalid input'; setNumberToGuess(Math.floor(Math.random() * 101)); correctGuesses = false; setResult(` ${result}`); } setGuess((prevGuess) => Math.max(0, prevGuess + 1)); let isWin = (value === numberToGuess && correctGuesses); switch (isWin) { case true: return ( <React.Fragment> <h2>Gaming</h2><br /> You Win. Congratulations<br /> ` function GuessingGame() { const [guess, setGuess] = useState(0); const [result, setResult] = useState(null); const [numberToGuess, setNumberToGuess] = useState(Math.floor(Math.random() * 201)); // generate a random number between 1 and 200 const handleGuess = (value) => { if (!isNaN(value)) { let correctGuesses = 0; if (value < numberToGuess) { result = 'Too low, try again! '; setNumberToGuess(Math.max(1, numberToGuess - Math.floor(value / 10))); } else if (value > numberToGuess) { result = 'Too high, try again!'; setNumberToGuess(1 + Math.min(200, numberToGuess + Math.ceil((value - numberToGuess) / 10))); } correctGuesses = value === numberToGuess; setResult(result); } else if (value !== '') { result = 'Invalid input'; setNumberToGuess(Math.floor(Math.random() * 201)); correctGuesses = false; setResult(` ${result}`); } setGuess((prevGuess) => Math.max(0, prevGuess + 1)); let isWin = (value === numberToGuess && correctGuesses); switch (isWin) { case true: return ( <React.Fragment> <h2>Gaming</h2><br /> You Win. Congratulations<br /> ` const guessNumbers = [20, 30]; let guessVal1 = guessNumbers[0]; let guessVal2 = guessNumbers[guessNumberToGuess]; if (!guessNumbers.length) { const newNumberOfGames = Number(guessNumers.at(0)+4); const newNumberOfGame2 = (guessNumberFrom+1); guessNumbers.splice(guessnewval1 + 1, (guessNumbers[guesstwoindex].length -10), ((newNumberOfGame2 +1).toString())); } result = ('Guess the numbers, you dont have a chance'); return ( <div> <h5> Guess number </h5><br /> <h6>{guess}</h6> <input type='Number' value={guessVal2} onChange={(e)=> {handleGuess(e.target.value)}}/> <span>Too low</span><span> Too high</span><br /> <span>{result} </span> <button onClick={() =>{handleWin()}}> Win </button> </div> ); } export default GuessingGame;"
